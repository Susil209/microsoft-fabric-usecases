## Daily Log – 2026-05-11

## 05_batch_scoring.py results + analysis

### Ensemble results (full dataset scoring)

- CRITICAL: 1,068 txns (0.37%) | 478 fraud | precision 44.76%
- HIGH:     26 txns (0.01%)    | 0 fraud   | ← tier removed
- MONITOR:  283,713 txns       | 14 missed

### Key findings

1. IF adds ZERO unique fraud catches (IF-only = 0)
   - All 133 IF catches are already caught by XGBoost
   - Root cause: PCA-transformed dataset — fraud already geometrically separated
   - Decision: keep IF in architecture for production streaming (novel fraud)
   - Remove from batch scoring ensemble for this dataset

2. HIGH tier catches nothing — no fraud scores 50-80%
   - Model is bimodal: very confident fraud or very confident legitimate
   - Removing HIGH tier from scoring logic

3. 97% capture rate is inflated (includes training data)
   - Honest figure from test set: 85.7% recall
   - This is the number to report to manager/client

4. Score distribution correct: median=0.0 (99.83% legit = near-zero score)

### Fixes applied to 05_batch_scoring.py

- Removed HIGH tier → two tiers only: CRITICAL / MONITOR
- Changed CRITICAL threshold: ≥0.70 (XGB) OR iso_flag=1
- Honest metrics now based on test set: Recall=85.7%, Precision=85.7%

### Revised pitch numbers

- Model catches 86 out of every 100 fraud transactions
- Only 14 false alerts per 56,962 transactions reviewed
- BLOCK_NOW: auto-block at ≥90% confidence + Amount ≥$500 (9 FP only)

### Next: 06_gold_layer.sql → update vw_critical_alerts

- Remove HIGH_DEMAND tier from SQL view
- Update threshold comment to 0.70